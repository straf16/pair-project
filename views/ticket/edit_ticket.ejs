<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://fonts.googleapis.com/css?family=Cinzel:400,700|Lora|Montserrat|Parisienne&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <%- include('../partials/style') -%>
    <title>Ticket Booking</title>
</head>
<body class="container">
    <div class="register">
    <h1 class="center judul center">Your Booked Ticket</h1><br>
    <form class="center" method="post" action="/editTicket">
        <div class="form-group isi row">
            <label for="name" class="col-sm-2 col-form-label">Booking Code</label>
            <div class="col-sm-10">
            <input type="text" class="form-control" name="name" value="<%= ticket.bookingCode %>">
            </div>
        </div>
        <div class="form-group  isi row">
            <label for="email" class="col-sm-2 col-form-label">Your Id</label>
            <div class="col-sm-10">
            <input type="text" class="form-control" name="email" value="<%= ticket.ViewerId %>">
            </div>
        </div>
        <div class="form-group  isi row">
            <select class="custom-select col-sm-12" name="cinemaId">
                <option selected>Choose Studio</option>
                <% for (let i = 0; i < cinemas.length; i++) { %>
                <option value="<%= cinemas[i].id %>"><%= cinemas[i].film %></option>
                <% } %>
            </select>
        </div>
        <input type="submit" class="btn-secondary isi btn" value="Edit">
        <a href="/cinemas" class="btn-secondary isi btn">Cancel</a>
</form>
</div>
</body>
</html>